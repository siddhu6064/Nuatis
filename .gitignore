# Dependencies
node_modules/
.npm

# Build outputs
dist/
.next/
out/
build/

# Environment files — NEVER commit these
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*.local

# TypeScript
*.tsbuildinfo

# Testing
coverage/
.nyc_output

# Logs
logs/
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/settings.json
.idea/
*.swp
*.swo

.claude/settings.local.json

# Vercel
.vercel

# Terraform (Phase 4)
*.tfstate
*.tfstate.backup
.terraform/
apps/web/.env.local
.claude/

# Git worktrees
.worktrees/
supabase/.temp/

PENDING_ENV_VARS.md
NUATIS_PENDING_TASKS.md
MIGRATIONS_TO_APPLY.md
audit-report.json
secrets-report.json
scratchpad/

# Swift Package Manager / Xcode build output (apps/ios)
.build/
DerivedData/

# Next regenerates next-env.d.ts on every dev start and every build, flipping
# its routes import between .next/dev/types and .next/types. Tracking it means
# a permanently dirty working tree in three apps and a file that re-appears in
# every `git add`.
next-env.d.ts
