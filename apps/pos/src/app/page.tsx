import Typography from '@mui/material/Typography'

// Placeholder — replaced by the register screen in Task 4.
export default function RegisterPage() {
  return (
    <main style={{ padding: 32 }}>
      <Typography variant="h4">Register</Typography>
    </main>
  )
}
