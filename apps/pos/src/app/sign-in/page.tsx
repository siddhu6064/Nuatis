import Typography from '@mui/material/Typography'

// Placeholder — replaced by the PIN pad in Task 3.
export default function SignInPage() {
  return (
    <main style={{ padding: 32 }}>
      <Typography variant="h4">Sign in</Typography>
    </main>
  )
}
